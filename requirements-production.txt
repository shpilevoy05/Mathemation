# Прод-зависимости: базовые плюс то, что нужно только на сервере.
# Отдельный файл, потому что gunicorn не ставится на Windows, а локальная
# разработка и тесты идут именно там.
-r requirements.txt

# WSGI-сервер. Django собственный сервер для прода не годится: он однопоточный,
# не переживает перезапуск воркеров и не рассчитан на внешний трафик.
gunicorn>=23.0
